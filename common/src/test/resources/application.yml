spring:
  application:
    name: argocd-tech-adapter

server:
  port: 8888

springdoc:
  swagger-ui:
    path: /docs


argocd:
  token: fake-token
  basePath: https://fake-url.com

git:
  username: user123
  token: git-token